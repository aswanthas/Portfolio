# Portfolio (single-page)

Open `index.html` in a browser. On macOS you can run:

```bash
open index.html
```

Edit the content in `index.html` to replace `Your Name` and `your-email@example.com` with your real name and email. Styles are in `styles.css` and behavior is in `script.js`.
